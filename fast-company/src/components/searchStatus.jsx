import React, { useState } from 'react';

const SearchStatus = () => {

    return (
        <>
            {users.length > 0 ? (
                <div>1</div>
            ) : (
                <div>2</div>
            )};
        </>
    );
};

export default SearchStatus;